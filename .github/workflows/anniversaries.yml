name: Daily Anniversary Emails

# Hits the production anniversary endpoint once a day. The route itself is
# idempotent — it skips employees whose AnniversaryEmail row for the current
# year already exists, so re-runs are safe.

on:
  schedule:
    # 14:00 UTC = 9 AM EST (US Eastern, winter / standard time).
    # NOTE: GitHub Actions cron is UTC only and does NOT shift for DST.
    # During EDT (US daylight time, roughly mid-March → early November)
    # this will fire at 10 AM ET. If you'd rather lock to EDT (9 AM most
    # of the year), change to '0 13 * * *'.
    - cron: '0 14 * * *'

  # Allow running it manually from the Actions tab as well
  workflow_dispatch:

jobs:
  trigger:
    runs-on: ubuntu-latest
    steps:
      - name: Call /api/cron/anniversaries
        run: |
          set -euo pipefail

          response=$(curl -sS -w "\nHTTP_STATUS:%{http_code}" \
            -H "Authorization: Bearer ${{ secrets.CRON_SECRET }}" \
            "${{ secrets.APP_URL }}/api/cron/anniversaries")

          body=$(echo "$response" | sed -e '$d')
          status=$(echo "$response" | tail -n1 | sed 's/HTTP_STATUS://')

          echo "Response body:"
          echo "$body"
          echo "HTTP status: $status"

          if [ "$status" != "200" ]; then
            echo "::error::Anniversary cron returned non-200 status"
            exit 1
          fi
